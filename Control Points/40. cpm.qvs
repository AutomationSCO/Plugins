cpm:
left keep(sale_basic)
LOAD
    sale_service_id,
    cpm_kpi_tt,
    tt_flag
FROM [lib://Geo_AMS/Automation/BI/Plugins/Reports/cpm/cpm_global.csv]
(txt, utf8, embedded labels, delimiter is ',', msq);